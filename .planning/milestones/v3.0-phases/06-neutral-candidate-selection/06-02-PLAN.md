---
phase: 06-neutral-candidate-selection
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - modules/local/parsefirstmapping/main.nf
  - modules/local/parsefirstmapping/tests/main.nf.test
  - modules/local/parsefirstmapping/tests/main.nf.test.snap
autonomous: true
requirements: [REFSEL-01, REFSEL-02]
must_haves:
  truths:
    - "PARSEFIRSTMAPPING emits the long-format candidate table on a new `candidates` channel while keeping major_mapping / minor_mapping / csv emits intact (D-06 shim)"
    - "PARSEFIRSTMAPPING passes params.n_candidates positionally to the selection script"
    - "The module stub emits both the legacy 10-column CSV and the new long-format candidates CSV so -stub-run fan-out works"
    - "The parsefirstmapping nf-test asserts the candidates emit and the regenerated snapshot reflects no-validity-filtering selection"
  artifacts:
    - path: "modules/local/parsefirstmapping/main.nf"
      provides: "candidates emit + n_candidates positional arg + updated stub"
      contains: "candidates"
    - path: "modules/local/parsefirstmapping/tests/main.nf.test"
      provides: "Updated nf-test assertions for the candidates emit and no-validity-filtering behavior"
      contains: "candidates"
    - path: "modules/local/parsefirstmapping/tests/main.nf.test.snap"
      provides: "Regenerated snapshot for the new emit contract"
  key_links:
    - from: "modules/local/parsefirstmapping/main.nf"
      to: "bin/summarize_mapping_to_all_references.R"
      via: "script invocation passing ${params.n_candidates} positionally"
      pattern: "params.n_candidates"
    - from: "modules/local/parsefirstmapping/main.nf"
      to: "workflows/hcvtyper.nf fan-out (Plan 03)"
      via: "candidates emit consumed by splitCsv fan-out"
      pattern: "emit: candidates"
---

<objective>
Extend the PARSEFIRSTMAPPING module to surface the long-format candidate table as a routable `candidates` channel and to pass the candidate-count param to the rewritten selection script, while keeping every legacy emit (`major_mapping`, `minor_mapping`, `csv`) intact under the D-06 shim. Update the module nf-test to assert the new emit and regenerate its snapshot to reflect the no-validity-filtering selection (T-3).

Purpose: The `candidates` emit is the channel contract the Plan-03 workflow fan-out consumes. It must exist before the routing collapse so the fan-out has a per-candidate row source. Implements the module-tier half of REFSEL-01 and wires REFSEL-02's param into the process. Depends on the long-format CSV schema locked in Plan 01.

Output: Updated `modules/local/parsefirstmapping/main.nf` (emit + script + stub), updated nf-test, regenerated snapshot.
</objective>

<execution_context>
@/home/jon.brate/hcvtyper/.claude/gsd-core/workflows/execute-plan.md
@/home/jon.brate/hcvtyper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-neutral-candidate-selection/06-CONTEXT.md
@.planning/phases/06-neutral-candidate-selection/06-RESEARCH.md
@.planning/phases/06-neutral-candidate-selection/06-PATTERNS.md
@.planning/phases/06-neutral-candidate-selection/06-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- **New channel emit:** `PARSEFIRSTMAPPING.out.candidates` (long-format candidate CSV, `*.candidates.csv` glob).
- **Module script change:** `${params.n_candidates}` passed positionally to `summarize_mapping_to_all_references.R`.
- **Updated stub:** emits both the legacy 10-column CSV and the long-format candidates CSV.
- **Regenerated test snapshot:** `modules/local/parsefirstmapping/tests/main.nf.test.snap`.
(Retained, unchanged emits: `major_mapping`, `minor_mapping`, `csv`, `versions` — D-06 shim.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the candidates emit + n_candidates arg + stub update to PARSEFIRSTMAPPING</name>
  <files>modules/local/parsefirstmapping/main.nf</files>
  <read_first>
    - modules/local/parsefirstmapping/main.nf (FULL — current emit contract lines 19-23, script invocation 32-40, stub 50-76; the self-analog to extend)
    - bin/summarize_mapping_to_all_references.R (the rewritten script from Plan 01 — confirm the long-format CSV filename glob and that it accepts the n_candidates positional arg)
    - .planning/phases/06-neutral-candidate-selection/06-01-SUMMARY.md (the exact long-format column header + filename glob Plan 01 chose)
    - 06-PATTERNS.md § "modules/local/parsefirstmapping/main.nf — ADD CANDIDATES EMIT"
    - 06-RESEARCH.md REFSEL-02 row + § "Recommended Project Structure"
    - 06-CONTEXT.md D-01 (long-format), D-06 (keep legacy emits)
  </read_first>
  <action>
    Add a new output `tuple val(meta), path("*.candidates.csv"), emit: candidates, optional: true` to the `output:` block, KEEPING the existing `major_mapping`, `minor_mapping`, `csv`, and `versions` emits unchanged (D-06). Ensure the legacy `*.csv` glob and the new `*.candidates.csv` glob do not collide — match the actual filenames the Plan-01 script writes (per 06-01-SUMMARY.md); if the legacy wide CSV is `*.parsefirstmapping.csv`, the `csv` emit glob must stay specific enough not to also capture `*.candidates.csv`. In the `script:` block, append `${params.n_candidates}` as the new positional argument after `${params.minCov}` (before `$args`). In the `stub:` block, additionally write a `${prefix}.candidates.csv` with the full long-format header (`sample,candidate_rank,candidate_ref,candidate_subtype,candidate_genotype,candidate_reads,candidate_cov,confirmation_status`) and at least two rows so a `-stub-run` of the workflow fans out into 2 candidates; keep the existing legacy 10-column stub CSV and FASTA touches.
  </action>
  <verify>
    <automated>cd /home/jon.brate/hcvtyper &amp;&amp; grep -q 'emit: candidates' modules/local/parsefirstmapping/main.nf &amp;&amp; grep -q 'params.n_candidates' modules/local/parsefirstmapping/main.nf &amp;&amp; grep -q 'candidates.csv' modules/local/parsefirstmapping/main.nf &amp;&amp; grep -q 'emit: major_mapping' modules/local/parsefirstmapping/main.nf &amp;&amp; grep -q 'emit: minor_mapping' modules/local/parsefirstmapping/main.nf &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `main.nf` has an `emit: candidates` output globbing `*.candidates.csv`.
    - The legacy `major_mapping`, `minor_mapping`, `csv`, `versions` emits are still present (grep confirms major_mapping + minor_mapping).
    - The `csv` (legacy wide) glob does not also capture `*.candidates.csv`.
    - The script block passes `${params.n_candidates}` positionally after `${params.minCov}`.
    - The stub writes a `*.candidates.csv` with the long-format header and ≥2 rows AND the legacy 10-column CSV.
  </acceptance_criteria>
  <done>PARSEFIRSTMAPPING emits the candidates channel, passes n_candidates to the R script, retains all legacy emits, and the stub emits both CSV shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Update parsefirstmapping nf-test for the candidates emit + no-validity-filtering, regenerate snapshot</name>
  <files>modules/local/parsefirstmapping/tests/main.nf.test, modules/local/parsefirstmapping/tests/main.nf.test.snap</files>
  <read_first>
    - modules/local/parsefirstmapping/tests/main.nf.test (FULL — existing GATE-02 lines ~147-188, GATE-03 ~76-106, GATE-04 ~110-138)
    - modules/local/parsefirstmapping/main.nf (the updated module from Task 1 — the new candidates emit to assert)
    - .planning/phases/06-neutral-candidate-selection/06-01-SUMMARY.md (expected long-format columns to assert)
    - 06-PATTERNS.md § "modules/local/parsefirstmapping/tests/main.nf.test (test, UPDATE) — T-3"
    - 06-RESEARCH.md § "Decision Tensions" T-3 (which cases change) and § "Validation Architecture" (snapshot churn expected, A4)
    - MEMORY.md notes: nf-test 0.9.3 invoked via `PATH=~/.nf-test:$PATH ... --profile docker`; host disk near-full (clean work/ + docker volume prune on ENOSPC)
  </read_first>
  <action>
    Add nf-test assertions that `process.out.candidates` is emitted and that its CSV header contains the long-format columns (`candidate_rank`, `candidate_ref`, `confirmation_status`). Update the GATE-03 (2k1b single-major no-minor) and GATE-04 (no-minor-FASTA) cases: under D-05 no-validity-filtering, a 2k1b / same-genotype-second-subtype sample may now select a 2nd candidate, so assertions like `minor_mapping.size() == 0` must be updated to the NEW neutral behavior (document the change inline referencing D-05). The GATE-02 failing-major case's direct CSV-cell assertions (`gate_flag`, `minor_call`, `major_ref`) LARGELY hold via the reconstructed shim columns — keep them, adjusting only if the reconstructed values genuinely differ. Regenerate the snapshot with `--update-snapshot`. Snapshot churn is EXPECTED (A4), not a regression. If the run hits ENOSPC, clean `work/` and `docker volume prune` first (MEMORY.md).
  </action>
  <verify>
    <automated>cd /home/jon.brate/hcvtyper &amp;&amp; grep -q 'candidates' modules/local/parsefirstmapping/tests/main.nf.test &amp;&amp; df -P . | awk 'NR==2{if($5+0>90){print "ERROR: disk >90% full (MEMORY.md) — run: sudo rm -rf work/ &amp;&amp; docker volume prune -f, then retry"; exit 1}}' &amp;&amp; PATH=~/.nf-test:$PATH nf-test test modules/local/parsefirstmapping/tests/main.nf.test --profile docker</automated>
  </verify>
  <acceptance_criteria>
    - The nf-test references `candidates` and asserts the long-format header columns.
    - GATE-03 / GATE-04 assertions reflect no-validity-filtering selection (D-05) with an inline note.
    - `PATH=~/.nf-test:$PATH nf-test test modules/local/parsefirstmapping/tests/main.nf.test --profile docker` passes.
    - The regenerated `.snap` is committed and reflects the new emit contract.
  </acceptance_criteria>
  <done>The parsefirstmapping nf-test asserts the candidates emit and the updated no-validity-filtering behavior; the snapshot is regenerated and green under --profile docker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `params.n_candidates` → process script interpolation | User-supplied integer interpolated into the process command line via `${params.n_candidates}` |
| selection-CSV / candidates-CSV → Nextflow channel | Module-produced CSVs become routable channel payloads consumed downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | `${params.n_candidates}` interpolated into the process command | mitigate | nf-schema validates it as a typed integer (≥1) before the process runs (declared in Plan 01); no string/shell content can reach the command line. |
| T-06-05 | Denial of Service | glob collision between `*.csv` (legacy) and `*.candidates.csv` (long) | mitigate | Make the `csv` emit glob specific (e.g. `*.parsefirstmapping.csv`) so the two outputs route to distinct channels; asserted by Task 1 acceptance criteria. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (already-present nf-test + container stack); no legitimacy checkpoint required. |
</threat_model>

<verification>
- `PATH=~/.nf-test:$PATH nf-test test modules/local/parsefirstmapping/tests/main.nf.test --profile docker` green.
- `candidates` emit present in `main.nf`; legacy emits retained (D-06 shim).
- Snapshot churn from D-05 no-validity-filtering is EXPECTED and regenerated, NOT treated as a regression (A4). Golden-baseline reproduction is NOT a Phase-6 gate (Phase 9 / COMPAT-01).
</verification>

<success_criteria>
- REFSEL-01: module exposes the neutral candidate set as a routable `candidates` channel.
- REFSEL-02: `params.n_candidates` flows into the selection script via the module.
- D-06: legacy emits + stub legacy CSV preserved.
- nf-test green under `--profile docker`; snapshot regenerated.
</success_criteria>

<output>
Create `.planning/phases/06-neutral-candidate-selection/06-02-SUMMARY.md` when done.
</output>
