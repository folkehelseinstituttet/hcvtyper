---
phase: 08-dominance-scoring-strain-role-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/classify_roles.R
  - bin/tests/test_dominance_score.R
  - bin/tests/test_classify_roles.R
  - nextflow.config
  - nextflow_schema.json
autonomous: true
requirements: [SCORE-01, SCORE-02, CLASS-01, CLASS-02, CLASS-03, CLASS-04]
must_haves:
  truths:
    - "score_candidates() ranks a genuine even minor above a high-read spiky cross-mapping artefact (4g loses to even minor)"
    - "classify_roles() assigns each candidate exactly one of dominant / co-infection / background"
    - "A non-dominant candidate clearing the floor with assembly support classifies co-infection/corroborated"
    - "A non-dominant candidate clearing the floor with NO support, when the dominant DID assemble substantially, classifies background/refuted_denovo (the false-4g case)"
    - "A non-dominant candidate with no support AND a dominant that also failed de novo is kept co-infection/uncorroborated_kept (genuine low-yield minor preserved)"
    - "Same-genotype (non-1a/1b) candidate or 2k1b pair classifies background per ported is_valid_minor()"
    - "overall sample call is exactly one of monoinfection / co-infection / indeterminate derived from roles"
    - "no-candidate / zero-row input returns a typed frame with role/dominance_score/role_reason columns, never aborts"
  artifacts:
    - path: "bin/classify_roles.R"
      provides: "Pure sourced helpers score_candidates() + classify_roles() + ported is_valid_minor()"
      contains: "classify_roles <- function"
    - path: "bin/tests/test_dominance_score.R"
      provides: "SCORE-01/02 unit coverage incl. the 4g-loses-to-even-minor headline assertion"
    - path: "bin/tests/test_classify_roles.R"
      provides: "CLASS-01..04 unit coverage incl. false-4g→background and genuine-coinfection→preserved"
  key_links:
    - from: "bin/tests/test_classify_roles.R"
      to: "bin/classify_roles.R"
      via: "source() of the real helper"
      pattern: "source.*classify_roles"
    - from: "bin/classify_roles.R"
      to: "bin/genotype_utils.R"
      via: "genotype_from_subtype() for genotype comparison"
      pattern: "genotype_from_subtype"
---

<objective>
Author the pure sourced R helper `bin/classify_roles.R` (dominance score + strain-role classifier + recovered `is_valid_minor()` exceptions), its two unit-test files calibrated against the handoff §2 evidence table, and reconcile the `denovo_*` corroboration-floor defaults + declare the new `score_weight_*` params. This plan delivers all of Phase 8's decision logic as a standalone, unit-testable artifact with NO pipeline run required — `bin/summarize.R` wiring is Plan 02.

Purpose: Phases 6 and 7 produced the evidence streams (neutral candidates, genotype-level assembly support, per-candidate coverage). This plan is the pure combination logic — score, role, sample call — that catches the index-hopping/cross-mapping false co-infections (the benchmarked false 4g) while preserving genuine low-abundance co-infections.
Output: `bin/classify_roles.R`, `bin/tests/test_dominance_score.R`, `bin/tests/test_classify_roles.R`, corrected `denovo_*` defaults + new `score_weight_*` params in `nextflow.config` + `nextflow_schema.json`.
</objective>

## Artifacts this phase produces

> Source-grounding reference for review-convergence. Symbols NEW in Phase 8 (exclude from drift verification):

**New files:**
- `bin/classify_roles.R` — pure sourced helper hosting `score_candidates()`, `classify_roles()`, and the ported `is_valid_minor()`
- `bin/tests/test_dominance_score.R`, `bin/tests/test_classify_roles.R` — new Rscript unit tests
- (optional) `bin/tests/fixtures/` evidence-table fixture rows

**New functions:**
- `score_candidates()` — computes per-candidate `dominance_score`
- `classify_roles()` — assigns `role` + `role_reason` + `overall_sample_call`
- `is_valid_minor()` — recovered verbatim from git `43904de~1` and embedded in `classify_roles.R`

**New CLI / ext.args params (declared this plan, wired Plan 02):**
- `score_weight_evenness`, `score_weight_reads`, `score_weight_kmercov`, and the evenness-transform constant (Claude's-discretion final names following the `score_weight_*` precedent)

**New Summary / candidate columns (emitted Plan 02, defined here as helper outputs):**
- `dominance_score`, `role` (dominant / co-infection / background), `role_reason`, `overall_sample_call`

**Reconciled (not new) config defaults:**
- `denovo_min_contig_length` 500→1000, `denovo_min_kmer_cov` 10.0→2.0 in `nextflow.config` + `nextflow_schema.json`

**Removed (Plan 02):** `minor_denovo_status`, `coinfection_flag` columns; `classify_minor_denovo` / `apply_denovo_layer` consumption.

<execution_context>
@/home/jon.brate/hcvtyper/.claude/gsd-core/workflows/execute-plan.md
@/home/jon.brate/hcvtyper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dominance-scoring-strain-role-classification/08-CONTEXT.md
@.planning/phases/08-dominance-scoring-strain-role-classification/08-RESEARCH.md
@.planning/phases/08-dominance-scoring-strain-role-classification/08-PATTERNS.md
@hcvtyper_handoff_denovo_informed_selection.md
@bin/assembly_support_join.R
@bin/denovo_confirm.R
@bin/genotype_utils.R
@bin/tests/test_assembly_support_join.R
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile denovo_* corroboration-floor defaults and declare score_weight_* params</name>
  <files>nextflow.config, nextflow_schema.json</files>
  <read_first>
    - nextflow.config (the `// De novo confirmation options` block ~L45-53 and `// Candidate selection options` ~L55)
    - nextflow_schema.json (the `denovo_min_contig_length` / `denovo_min_kmer_cov` / `n_candidates` property entries ~L191-225)
    - bin/denovo_confirm.R (the validated 1000/2.0/90 helper defaults at L36-38)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-RESEARCH.md (Pitfall 1 — calibration-default mismatch; Open Question #2)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-PATTERNS.md (Shared Patterns → Calibration-default mismatch)
  </read_first>
  <action>
    Resolve RESEARCH Open Question #1 / Pitfall 1 (calibration-default mismatch). In `nextflow.config` change `denovo_min_contig_length` from `500` to `1000` and `denovo_min_kmer_cov` from `10.0` to `2.0` so the SHIPPED corroboration floor equals the 03-RESEARCH-validated 1000/2.0/90 that D-10 assumes. Leave `denovo_min_blast_identity = 90` unchanged. Rationale to put in the inline comment: the runtime `ext.args` carries these config values and overrides the helper defaults, so the effective D-10 corroboration verdict floor is whatever the config ships; 10.0 is STRICTER than validated 2.0 and would wrongly refute ERR1810453's genuine partial 2b (k-mer cov ~5) that CLASS-02 must preserve. Update the matching `default` keys in `nextflow_schema.json` for `denovo_min_contig_length` (→1000) and `denovo_min_kmer_cov` (→2.0).

    In the same `nextflow.config` block, declare the new dominance-score weight params with calibrated defaults so breadth-evenness out-weighs raw read count (SCORE-02, D-02/D-04): `score_weight_evenness`, `score_weight_reads`, `score_weight_kmercov`, and the evenness-transform constant (name at your discretion per the `score_weight_*` precedent, e.g. `score_evenness_k`). Set `score_weight_evenness` strictly greater than `score_weight_reads`. These defaults are calibrated against the handoff §2 evidence table; if the real per-contig numbers are unavailable, calibrate against the table values + the test fixtures from Task 3 and add an inline comment flagging them for the analyst validation pass (consistent with REQUIREMENTS.md out-of-scope "analyst validates afterward"). Add matching typed `number` properties (type/default/description) to `nextflow_schema.json` mirroring the `denovo_min_contig_length` entry shape. Do NOT touch `conf/modules_hcv.config` or the `summarize.R` arg parse here — the positional `ext.args` wiring is Plan 02 (Pitfall 2: new args append at the END only).
  </action>
  <verify>
    <automated>grep -E 'denovo_min_contig_length\s*=\s*1000|denovo_min_kmer_cov\s*=\s*2\.0' nextflow.config | grep -cv '^\s*//' | grep -qv '^0$' && grep -c 'score_weight_evenness' nextflow.config</automated>
  </verify>
  <acceptance_criteria>
    - `nextflow.config` sets `denovo_min_contig_length = 1000` and `denovo_min_kmer_cov = 2.0` (not 500 / 10.0)
    - `nextflow.config` declares `score_weight_evenness`, `score_weight_reads`, `score_weight_kmercov`, and an evenness-transform constant, with `score_weight_evenness` numerically greater than `score_weight_reads`
    - `nextflow_schema.json` `default` for `denovo_min_contig_length` is `1000` and for `denovo_min_kmer_cov` is `2.0`, and contains property entries for each new `score_weight_*` param
    - `conf/modules_hcv.config` and `bin/summarize.R` are NOT modified in this task
  </acceptance_criteria>
  <done>Shipped corroboration floor equals validated 1000/2.0/90; new score-weight params declared with evenness-dominant defaults; schema in sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author bin/classify_roles.R — score_candidates() + classify_roles() + recovered is_valid_minor()</name>
  <files>bin/classify_roles.R</files>
  <behavior>
    - score_candidates(df, score_weights, evenness_const): returns df with a numeric `dominance_score` per candidate = weighted sum of log10(reads), breadth fraction (0-1), CV-evenness factor (0-1), and a bonus-only log(k-mer_cov) term; evenness weight dominates (D-02/D-03/D-05). k-mer-cov term is capped and ONLY positive — NA/none assembly support yields no boost and never a penalty (D-05).
    - classify_roles(scored_df, minRead, minCov, denovo_min_contig_length=1000, denovo_min_kmer_cov=2.0, denovo_min_blast_identity=90, match_level="genotype"): returns scored_df with `role` (exactly one of dominant/co-infection/background), `role_reason` (coded vocabulary), and an `overall_sample_call` value.
    - dominant = highest dominance_score among candidates that ALSO pass the minRead/minCov gate (D-01); deterministic tie-break score → reads → ref name (D-06); if none pass the gate, no dominant and overall_sample_call="indeterminate".
    - Each non-dominant candidate: clears floor (minRead/minCov, D-07/D-09) AND has genotype-level assembly support (ANDed denovo floors at match_level, D-10) → role=co-infection, role_reason=corroborated. Clears floor but no own support AND the DOMINANT assembled substantially → role=background, role_reason=refuted_denovo (D-11 asymmetric refute, the 4g case). Clears floor, no own support, dominant ALSO did not assemble substantially → role=co-infection, role_reason=uncorroborated_kept (D-11 — never suppress on inconclusive de novo). Below floor → role=background, role_reason=below_floor.
    - HCV exceptions (D-12, ported is_valid_minor verbatim): a co-infection candidate that is same-genotype-as-dominant (non-1a/1b) → role=background, role_reason=same_genotype_as_dominant; a 2k1b pair → role=background, role_reason=recombinant_2k1b. Exceptions may only DEMOTE to background, never promote (RESEARCH anti-pattern).
    - overall_sample_call (D-14): >=1 co-infection role (incl. uncorroborated_kept) → "co-infection"; exactly 1 dominant + only background → "monoinfection"; no candidate passes the major-gate → "indeterminate".
    - Zero-row / NULL input → typed zero-row frame carrying role (character), dominance_score (double), role_reason (character), overall_sample_call (character); never stop().
  </behavior>
  <read_first>
    - bin/assembly_support_join.R (house pattern: header L1-35, defensive tidyverse guard L37-42, match_level stopifnot L50, typed zero-row guard L63-80)
    - bin/denovo_confirm.R (floors-as-named-args L36-38, ANDed substantiality L62-66, asymmetric-refute return order L68-88)
    - bin/genotype_utils.R (genotype_from_subtype() L24-26, 2k1b-aware)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-PATTERNS.md (the recovered is_valid_minor() verbatim text + the exception predicate mapping)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-CONTEXT.md (D-01 through D-16 — the locked decision spec)
    - hcvtyper_handoff_denovo_informed_selection.md §2 (evidence table), §4 (asymmetric rule), §5 (substantial-contig thresholds + genotype-vs-subtype match)
  </read_first>
  <action>
    Recover the canonical `is_valid_minor()` body verbatim from git: `git show 43904de~1:bin/summarize_mapping_to_all_references.R` (the function starts at L112; it was removed in commit `43904de` "feat(06-01)"). Reconstruct it as a PURE function taking explicit `cand_subtype`/`cand_genotype`/`dom_subtype`/`dom_genotype` args instead of the old `major_*` closure vars, preserving the three rules EXACTLY (COMPAT-04): (1) allow 1a/1b cross-subtype as co-infection; (2) block 2k1b paired with genotype {1,2,2k1b}; (3) otherwise require different genotype. Use `genotype_from_subtype()` for genotype comparisons — do NOT hand-roll substr.

    Create `bin/classify_roles.R` following the `bin/assembly_support_join.R` house pattern: a doc-comment block stating purpose + purity contract, the defensive `if (!exists("group_by")) library(tidyverse)` guard, functions ONLY (no commandArgs, no top-level file I/O, no global mutation). Define `score_candidates()` and `classify_roles()` with the signatures and behaviors in <behavior>, plus the embedded `is_valid_minor()`. Add `stopifnot(match_level %in% c("genotype","subtype"))` (copy from assembly_support_join.R:50). Compute the dominance score as the D-02 normalized weighted sum: `log10(candidate_reads)`, breadth fraction (from `cand_cov_breadth` / 100 or the min_1 breadth column — your discretion per D-02), the pre-computed `cv_evenness` factor (0-1, supplied by the cov loop in Plan 02 — accept it as an input column; the helper does NOT read depth files), and a capped bonus-only `log(best_contig_kmer_cov)` term (D-05). The corroboration verdict reuses the ANDed denovo floors against the candidate's joined `assembly_support` columns (`best_contig_length`/`_kmer_cov`/`_pident`) at `match_level` (D-10). The asymmetric-refute step runs AFTER the dominant is chosen (Pitfall 5 — "did de novo work for the dominant?" reads the DOMINANT's own joined support metrics against the same floors). Generalize the typed zero-row guard from assembly_support_join.R:63-80 to the new `role`/`dominance_score`/`role_reason`/`overall_sample_call` columns. Do NOT source this file into summarize.R here — that is Plan 02.
  </action>
  <verify>
    <automated>Rscript -e 'source("bin/genotype_utils.R"); source("bin/classify_roles.R"); stopifnot(exists("score_candidates"), exists("classify_roles"), exists("is_valid_minor")); cat("OK\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `bin/classify_roles.R` contains `classify_roles <- function`, `score_candidates <- function`, and `is_valid_minor <- function`
    - File contains the defensive guard `if (!exists("group_by"))` and `stopifnot(match_level %in% c("genotype", "subtype"))`
    - File contains NO `commandArgs` and NO top-level `read_csv`/`write_csv`/`read_tsv` (pure helper)
    - `Rscript -e 'source("bin/genotype_utils.R"); source("bin/classify_roles.R")'` exits 0 and all three functions exist
    - `is_valid_minor()` body preserves the 1a/1b allow + 2k1b-vs-{1,2,2k1b} block + different-genotype rules recovered from `43904de~1`
  </acceptance_criteria>
  <done>classify_roles.R sources cleanly, exposes the three functions, is pure, and encodes D-01..D-14 role logic + the verbatim D-12 exceptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author unit tests asserting the handoff evidence-table calibration outcomes</name>
  <files>bin/tests/test_dominance_score.R, bin/tests/test_classify_roles.R</files>
  <behavior>
    - test_dominance_score.R: score monotonic in reads/breadth/kmer_cov holding others fixed; THE HEADLINE: a high-read spiky candidate (4g: 53279 reads, breadth 0.677, high CV → low cv_evenness) scores LOWER than a genuine even minor with fewer reads (SCORE-02 evenness-dominates-reads); NA/none k-mer cov gives no penalty vs a baseline (D-05).
    - test_classify_roles.R: false 4g (no 4g contig, dominant 1a HAS a substantial contig) → role=="background" && role_reason=="refuted_denovo"; ERR1810447 full 9207bp 2b and ERR1810453 2949bp partial 2b (k-mer cov ~5) → role=="co-infection" && role_reason=="corroborated"; sim1 1a:1b + sim2 2a:3a true co-infections preserved as co-infection; IVT extreme-ratio genuine minor where de novo failed for BOTH → role=="co-infection" && role_reason=="uncorroborated_kept"; same-genotype non-1a/1b candidate → background/same_genotype_as_dominant; 2k1b pair → background/recombinant_2k1b; no-candidate-passes-gate → overall_sample_call=="indeterminate"; each candidate has exactly one role (CLASS-01); zero-row input returns typed frame (CLASS-03 no row loss).
  </behavior>
  <read_first>
    - bin/tests/test_assembly_support_join.R (exact template: self-locating source block L24-39, mk_*() tibble builders L42-67, fail()/ok() assertions)
    - bin/tests/run_all.sh (auto-globs test_*.R; no registration needed)
    - bin/classify_roles.R (the real functions under test — written in Task 2)
    - hcvtyper_handoff_denovo_informed_selection.md §2 (the evidence-table numbers that are the calibration anchor)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; Code Examples → evidence-table calibration assertion)
  </read_first>
  <action>
    Copy the self-locating source block verbatim from `bin/tests/test_assembly_support_join.R:24-39`, adding `source(file.path(bin_dir, "classify_roles.R"))` AFTER `source(file.path(bin_dir, "genotype_utils.R"))`. Reuse the `mk_*()` in-memory tibble-builder idiom (L42-67) to construct candidate frames with the joined assembly-support columns + the `cv_evenness` factor column. Assert on the REAL sourced `score_candidates()` / `classify_roles()` — NEVER inline re-implement the logic (RESEARCH anti-pattern). Build the evidence-table fixture rows inline (or under `bin/tests/fixtures/` if a file is cleaner) for: false 4g refute, ERR1810447 + ERR1810453 2b confirm, sim1 1a:1b + sim2 2a:3a preserved, IVT uncorroborated_kept, the D-12 same-genotype and 2k1b exception cases, and the no-dominant indeterminate case. Use `fail(msg)`/`ok(msg)` (quit status 1 on fail) so `run_all.sh` propagates. If the calibrated weights from Task 1 do not make the 4g lose to the even minor, adjust the Task-1 weight defaults (not the test) until the evidence-table assertions pass — the weights are the calibration target, the table is the anchor.
  </action>
  <verify>
    <automated>bash bin/tests/run_all.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bin/tests/test_dominance_score.R` and `bin/tests/test_classify_roles.R` exist and `source()` the real `bin/classify_roles.R` (no inline re-implementation)
    - `bash bin/tests/run_all.sh` exits 0 with PASS lines for both new test files
    - test_dominance_score.R asserts the 4g candidate scores lower than a genuine even minor (SCORE-02)
    - test_classify_roles.R asserts: false-4g → role=="background" && role_reason=="refuted_denovo"; genuine partial 2b → role=="co-infection" && role_reason=="corroborated"; IVT-de-novo-failed → role_reason=="uncorroborated_kept"; no-gate-pass → overall_sample_call=="indeterminate"
    - test_classify_roles.R asserts the D-12 exceptions: same-genotype-non-1a/1b → background/same_genotype_as_dominant and 2k1b pair → background/recombinant_2k1b
  </acceptance_criteria>
  <done>`bash bin/tests/run_all.sh` green; the benchmarked false-4g (→background) and genuine partial co-infection (→preserved) cases are both asserted against the real helper, calibrating the Task-1 weights.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline-internal CSV/TSV → summarize.R | Upstream-generated candidate/support/depth files; trusted, but may be empty/malformed on no-mapping / skip-assembly runs |
| tracked config params → R args | `score_weight_*` / `denovo_*` values from version-controlled config; trusted, no external user input |

This phase processes only trusted pipeline-internal data produced by earlier stages and parameters from tracked config. There is NO authentication, session, network, secret, or external-user-input surface. Per ASVS L1 with `security_block_on: high`, there are NO high-severity threats. The residual low-severity items below are DoS-via-malformed-internal-data and config-tampering, both already covered by existing house patterns.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | classify_roles() / score_candidates() on zero-row or NULL candidate frame | mitigate | Typed zero-row guard generalized from assembly_support_join.R:63-80 — return a typed frame with role/dominance_score/role_reason/overall_sample_call, never stop() |
| T-08-02 | Denial of Service | score_candidates() on zero-mean / zero-length coverage candidate | mitigate | Guard `mean==0`/`ref_length==0` → cv_evenness factor 0 (computed in Plan 02 cov loop); helper accepts the pre-guarded factor, no NaN/Inf score |
| T-08-03 | Tampering (config) | malformed score_weight_* arg silently changing scoring behaviour | mitigate (Plan 02) | Defensive index-guarded `as.numeric(args[N])` parse with default fallback; new args appended at END only (Pitfall 2) — wired in Plan 02 |
| T-08-SC | Tampering | npm/pip/cargo/conda installs | accept | No package installs occur this phase — all R libs pre-provisioned in the pinned SUMMARIZE container; no new dependency surface |

No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — Phase 8 installs nothing. `security_block_on: high` satisfied.
</threat_model>

<verification>
- `bash bin/tests/run_all.sh` exits 0 (all R units incl. the two new test files green)
- `Rscript -e 'source("bin/genotype_utils.R"); source("bin/classify_roles.R")'` exits 0
- `grep` confirms reconciled denovo floors (1000/2.0) and declared score_weight_* params in nextflow.config + nextflow_schema.json
- The false-4g→background and genuine-partial-2b→co-infection evidence-table cases are asserted by test_classify_roles.R (VALIDATION.md Wave 0 requirement)
</verification>

<success_criteria>
- `bin/classify_roles.R` exposes pure `score_candidates()` + `classify_roles()` + verbatim-recovered `is_valid_minor()`; sources cleanly
- Dominance score weights breadth-evenness strongly above raw reads (SCORE-02); 4g loses to even minor in the unit test
- All three roles + 3-value sample call assignable; D-11 asymmetric refute and D-12 exceptions encoded
- Shipped denovo corroboration floor reconciled to validated 1000/2.0/90 (Pitfall 1 resolved)
- `score_weight_*` params declared in config + schema (ext.args wiring deferred to Plan 02)
- `bash bin/tests/run_all.sh` green
</success_criteria>

<output>
Create `.planning/phases/08-dominance-scoring-strain-role-classification/08-01-SUMMARY.md` when done
</output>
