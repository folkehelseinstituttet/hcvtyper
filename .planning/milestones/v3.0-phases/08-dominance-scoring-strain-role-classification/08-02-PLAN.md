---
phase: 08-dominance-scoring-strain-role-classification
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - bin/summarize.R
  - conf/modules_hcv.config
  - modules/local/summarize/main.nf
  - workflows/hcvtyper.nf
autonomous: true
requirements: [SCORE-01, SCORE-02, CLASS-01, CLASS-02, CLASS-03, CLASS-04]
must_haves:
  truths:
    - "summarize.R sources classify_roles.R and runs score_candidates() + classify_roles() over the Phase-7-joined candidate frame"
    - "A cv_evenness factor is computed inside the cov loop from per-position depth (cov$X3) before X3 is discarded"
    - "score_weight_* params reach summarize.R via ext.args appended AFTER n_candidates (no positional re-map)"
    - "Summary.csv carries overall_sample_call and no longer carries minor_denovo_status"
    - "The enriched long *.candidates.csv is written with every candidate incl. background + role + dominance_score + role_reason"
    - "The legacy apply_denovo_layer / classify_minor_denovo / coinfection_flag path is retired (no two confirmation systems)"
    - "review_flag is rewired onto role / role_reason / overall_sample_call"
    - "classify_roles.R is staged as a path() input to the SUMMARIZE module and passed at the workflow call site"
  artifacts:
    - path: "bin/summarize.R"
      provides: "Integrated dominance score + role classifier + sample call + rewired review_flag + retired legacy layer"
      contains: "classify_roles"
    - path: "modules/local/summarize/main.nf"
      provides: "classify_roles.R staged input + updated stub header (overall_sample_call in, minor_denovo_status out)"
      contains: "overall_sample_call"
    - path: "conf/modules_hcv.config"
      provides: "score_weight_* appended after n_candidates in SUMMARIZE ext.args"
      contains: "score_weight_evenness"
  key_links:
    - from: "bin/summarize.R"
      to: "bin/classify_roles.R"
      via: "source() then classify_roles() over candidate_support"
      pattern: "source.*classify_roles"
    - from: "conf/modules_hcv.config"
      to: "bin/summarize.R"
      via: "positional ext.args appended after n_candidates"
      pattern: "score_weight"
    - from: "modules/local/summarize/main.nf"
      to: "bin/classify_roles.R"
      via: "path() staging"
      pattern: "classify_roles"
---

<objective>
Wire the Plan-01 `classify_roles.R` helper into the terminal SUMMARIZE step: compute the `cv_evenness` factor inside the existing cov loop, source + run the classifier over the Phase-7-joined candidate frame, retire the legacy `apply_denovo_layer`/`minor_denovo_status`/`coinfection_flag` path (D-15), rewire `review_flag` onto roles, write the enriched long `*.candidates.csv` (CLASS-03), append `score_weight_*` to the SUMMARIZE `ext.args`, stage the new helper, and update the module stub header. This is the integration wave — it produces the actual `Summary.csv` / `*.candidates.csv` outputs.

Purpose: Turn the pure scoring/classification logic from Plan 01 into live pipeline behaviour, replacing the minor-coupled 2-strain confirmation model with the N-candidate role model so backgrounds are surfaced (not dropped) and one overall sample call is reported per sample.
Output: modified `bin/summarize.R`, `conf/modules_hcv.config`, `modules/local/summarize/main.nf`, `workflows/hcvtyper.nf`; enriched `Summary.csv` + `*.candidates.csv` schema.
</objective>

## Artifacts this phase produces

> Source-grounding reference for review-convergence. Symbols NEW in Phase 8 (exclude from drift verification):

**New / changed Summary + candidate columns:**
- ADDED to `Summary.csv` (wide): `overall_sample_call`
- ADDED to long `*.candidates.csv`: `role` (dominant / co-infection / background), `dominance_score`, `role_reason`
- ADDED internal column in `summarize.R` cov loop: `cv_evenness`
- REMOVED from `Summary.csv` / `summary_mqc.tsv` / module stub: `minor_denovo_status`, `coinfection_flag`

**New ext.args params wired (declared Plan 01):**
- `score_weight_evenness`, `score_weight_reads`, `score_weight_kmercov`, evenness-transform constant — appended AFTER `${params.n_candidates}` in `conf/modules_hcv.config:328`, parsed at `args[12]+` in `summarize.R`

**New staged input:**
- `classify_roles.R` — `path()` input on `modules/local/summarize/main.nf`, passed from `workflows/hcvtyper.nf` SUMMARIZE call site (mirrors `assembly_support_join.R` at L531)

**Retired (consumption removed, D-15):**
- `apply_denovo_layer()` (denovo_layer.R), `classify_minor_denovo()` (denovo_confirm.R) — files may remain staged; consumption removed
- `review_flag` triggers rewired off `minor_denovo_status`/`coinfection_flag`/`gate_flag` onto `role`/`role_reason`/`overall_sample_call`

**Deferred (NOT this phase):** nf-test snapshot regen for the new schema (fold into the already-deferred Phase-7 SUMMARIZE regen — docker/disk constrained); `.major.`/`.minor.` filename migration + `Major_*`/`Minor_*` column aliasing + full golden-baseline CI suite → Phase 9.

<execution_context>
@/home/jon.brate/hcvtyper/.claude/gsd-core/workflows/execute-plan.md
@/home/jon.brate/hcvtyper/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dominance-scoring-strain-role-classification/08-CONTEXT.md
@.planning/phases/08-dominance-scoring-strain-role-classification/08-RESEARCH.md
@.planning/phases/08-dominance-scoring-strain-role-classification/08-PATTERNS.md
@.planning/phases/08-dominance-scoring-strain-role-classification/08-01-PLAN.md
@bin/summarize.R
@bin/assembly_support_join.R
@conf/modules_hcv.config
@modules/local/summarize/main.nf
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute cv_evenness in the cov loop + append score_weight_* to ext.args and the arg parse</name>
  <files>bin/summarize.R, conf/modules_hcv.config</files>
  <behavior>
    - A new `cv_evenness` column on tmp_df holds `1/(1+sd(cov$X3)/mean(cov$X3))` over the full reference (zeros included, SAMTOOLS_DEPTH runs -aa), guarded so a zero-mean / zero-length candidate gets cv_evenness = 0 (no NaN/Inf).
    - cv_evenness is carried through the df_coverage pivot into per-candidate slots so the classifier can read it.
    - score_weight_evenness/reads/kmercov + evenness constant parse from args[12]+ with the existing defensive `if (length(args) >= N && nchar(args[N]) > 0) ... else <default>` form; n_candidates at args[11] is unaffected.
  </behavior>
  <read_first>
    - bin/summarize.R (sources + arg parse L1-46, the cov loop L355-420 incl. tmp_df ncol/colnames at L357-358 and avg_depth at L379, the df_coverage pivot L418-441)
    - conf/modules_hcv.config (the SUMMARIZE ext.args line L328 — the denovo_* + minRead/minCov + n_candidates positional string)
    - conf/modules_hcv.config (SAMTOOLS_DEPTH `-aa` at L259 — confirms zeros are present)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-PATTERNS.md (Pattern 2 CV-evenness-in-loop; Shared Patterns → Positional ext.args coupling)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-RESEARCH.md (Pitfall 2 positional coupling; Pitfall 3 zero-mean guard)
  </read_first>
  <action>
    In `bin/summarize.R`, widen `tmp_df` from `ncol = 7` to `ncol = 8` (L357) and add `"cv_evenness"` to its `colnames(...)` vector (L358). Inside the cov loop, immediately after `tmp_df$avg_depth[i] <- mean(cov$X3)` (L379), compute the D-03 evenness factor from the SAME `cov$X3` vector before it is discarded at loop end: assign `cv_raw` = `sd(cov$X3)/mean(cov$X3)` only when `ref_length > 0 && mean(cov$X3) > 0` else `NA_real_`, then `tmp_df$cv_evenness[i]` = `1/(1+cv_raw)` when not NA else `0` (guard the zero-mean edge per Pitfall 3). Carry `cv_evenness` through the `df_coverage` pivot at L418-441 alongside the existing breadth/avg_depth columns so each candidate slot has its evenness factor available to the classifier.

    In the arg-parse block (after `n_candidates` at L45), append the defensive parse for the new weights at `args[12]`+ using the EXACT existing guard form (`if (length(args) >= N && nchar(args[N]) > 0) as.numeric(args[N]) else <default>`): `score_weight_evenness` at args[12], `score_weight_reads` at args[13], `score_weight_kmercov` at args[14], and the evenness-transform constant at args[15], with defaults matching the nextflow.config values from Plan 01. In `conf/modules_hcv.config` SUMMARIZE `ext.args` (L328), append the four `${params.score_weight_*}` / evenness-constant interpolations (use the EXACT param names declared in Plan 01) AFTER `${params.n_candidates}` — NEVER mid-string (Pitfall 2: the R side reads args by index; inserting mid-string silently re-maps every later position). Do NOT yet call the classifier — that is Task 2.
  </action>
  <verify>
    <automated>grep -q 'cv_evenness' bin/summarize.R && grep -q 'score_weight_evenness' bin/summarize.R && grep -E 'n_candidates.*score_weight_evenness' conf/modules_hcv.config && Rscript -e 'invisible(parse("bin/summarize.R")); cat("PARSE_OK\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `bin/summarize.R` cov loop assigns `tmp_df$cv_evenness[i]` computed from `cov$X3` with a zero-mean/zero-length guard (cv_evenness = 0 in that branch)
    - `tmp_df` is created with `ncol = 8` and `cv_evenness` is in its `colnames(...)` vector
    - `bin/summarize.R` parses `score_weight_evenness`/`score_weight_reads`/`score_weight_kmercov` + evenness constant from `args[12]..args[15]`; `n_candidates` still reads `args[11]`
    - `conf/modules_hcv.config` SUMMARIZE `ext.args` ends with the `score_weight_*` interpolations AFTER `${params.n_candidates}` (verified by `grep -E 'n_candidates.*score_weight_evenness'`)
    - `Rscript -e 'parse("bin/summarize.R")'` succeeds (no syntax error)
  </acceptance_criteria>
  <done>cv_evenness is captured inside the cov loop before X3 is discarded; score_weight_* flows config→args without re-mapping n_candidates; summarize.R still parses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the classifier, retire the legacy layer, rewire review_flag, write enriched candidates.csv</name>
  <files>bin/summarize.R</files>
  <behavior>
    - summarize.R sources classify_roles.R (after assembly_support_join.R) and runs score_candidates() + classify_roles() over the long candidate_support frame; each candidate gets role / dominance_score / role_reason.
    - The legacy apply_denovo_layer() call + the coinfection_flag mutate are REMOVED; minor_denovo_status no longer produced.
    - dominant fills Major_* slots, corroborated co-infection fills Minor_* slots; overall_sample_call is a new wide column.
    - The enriched long *.candidates.csv (every candidate incl. background + role + dominance_score + role_reason) is written.
    - review_flag is rewired: pmap_chr over role / role_reason / overall_sample_call producing the derived human sentences; minor_denovo_status / coinfection_flag dropped from the select() and write schema; overall_sample_call added.
  </behavior>
  <read_first>
    - bin/summarize.R (source block L10-16; candidate read + join L505-585; the apply_denovo_layer call + coinfection_flag mutate L965-985; the review_flag pmap_chr L1096-1124; the column-reorder select L1127-1164; write_csv Summary.csv L1167 + summary_mqc.tsv L1181-1187; the candidates.csv read-only site ~L511-529 col_types precedent)
    - bin/classify_roles.R (the Plan-01 helper — score_candidates() / classify_roles() signatures + the cv_evenness / assembly_support columns it expects)
    - bin/assembly_support_join.R (the join output structure feeding candidate_support)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-CONTEXT.md (D-13 role_reason + derived sentence; D-15 retire legacy; D-16 wide-Summary + long-candidate detail)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-PATTERNS.md (summarize.R MODIFY edit sites 1-5; pmap_chr sentence builder)
  </read_first>
  <action>
    Add `source("classify_roles.R")` to the source block at L16 (AFTER `source("assembly_support_join.R")` so genotype_from_subtype is already in scope). After the Phase-7 `candidate_support <- join_assembly_support(...)` at L579, run `score_candidates(candidate_support, score_weights=list(evenness=score_weight_evenness, reads=score_weight_reads, kmercov=score_weight_kmercov), evenness_const=...)` then `classify_roles(..., minRead, minCov, denovo_min_contig_length, denovo_min_kmer_cov, denovo_min_blast_identity, denovo_match_level)` (the helper consumes the wide cand_* assembly-support columns + cv_evenness already on the joined frame). The classifier returns the long candidate frame with `role` / `dominance_score` / `role_reason` plus a per-sample `overall_sample_call`.

    REMOVE the legacy `apply_denovo_layer(...)` call AND the subsequent `coinfection_flag` mutate at L965-985 (D-15 — no two confirmation systems). Map the new roles into the existing wide layout (D-16): the `dominant` candidate fills the `Major_*` slots and the corroborated `co-infection` fills the `Minor_*` slots; add `overall_sample_call` as a new wide column on `final`. Write the enriched long `*.candidates.csv` (D-16, CLASS-03 — every candidate incl. background, with role/dominance_score/role_reason) via a new `write_csv(...)` of the classified candidate_support frame (the file is currently read-only; pin col_types on read per L519-528 if it is re-read).

    Rewire `review_flag` (L1096-1124): keep the `pmap_chr` full-sentence idiom but swap the trigger columns from `minor_denovo_status`/`coinfection_flag`/`gate_flag`/`denovo_*_subtype_match` onto `role`/`role_reason`/`overall_sample_call`, emitting the derived human sentences per the D-13 controlled vocabulary (e.g. refuted_denovo → "Minor strain candidate refuted by de novo assembly...", uncorroborated_kept → an analyst-review sentence). Update the column-reorder `select()` (L1127-1164): drop `minor_denovo_status` and `coinfection_flag`, add `overall_sample_call` (and any new role columns you surface in the wide Summary). The `write_csv(final, "Summary.csv")` (L1167) and `summary_mqc.tsv` schema (L1181-1187) follow the select automatically.
  </action>
  <verify>
    <automated>grep -q 'source("classify_roles.R")' bin/summarize.R && grep -q 'overall_sample_call' bin/summarize.R && grep -vc '^\s*#' bin/summarize.R >/dev/null && ! grep -E '^[^#]*apply_denovo_layer\(' bin/summarize.R && Rscript -e 'invisible(parse("bin/summarize.R")); cat("PARSE_OK\n")'</automated>
  </verify>
  <acceptance_criteria>
    - `bin/summarize.R` contains `source("classify_roles.R")` and calls `score_candidates(` and `classify_roles(`
    - `bin/summarize.R` contains `overall_sample_call` and writes the enriched long `*.candidates.csv` with role/dominance_score/role_reason
    - No uncommented `apply_denovo_layer(` call remains and `coinfection_flag` is not added to `final` (D-15)
    - The `select()` column-reorder no longer lists `minor_denovo_status` / `coinfection_flag` and includes `overall_sample_call`
    - `review_flag` `pmap_chr` reads `role` / `role_reason` / `overall_sample_call` (not `minor_denovo_status`)
    - `Rscript -e 'parse("bin/summarize.R")'` succeeds
  </acceptance_criteria>
  <done>The classifier runs in summarize.R, the legacy confirmation layer is retired, review_flag is role-driven, Summary.csv gains overall_sample_call and loses minor_denovo_status, and every candidate (incl. background) is written to the long candidates.csv.</done>
</task>

<task type="auto">
  <name>Task 3: Stage classify_roles.R in the SUMMARIZE module + update the stub header</name>
  <files>modules/local/summarize/main.nf, workflows/hcvtyper.nf</files>
  <read_first>
    - modules/local/summarize/main.nf (the helper `path()` inputs L30-33; the stub Summary.csv heredoc header L72 + summary_mqc.tsv printf header L77 — both end in `...denovo_minor_contig_length,minor_denovo_status`)
    - workflows/hcvtyper.nf (the SUMMARIZE call site — `assembly_support_join.R` passed at L531)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-PATTERNS.md (modules/local/summarize/main.nf MODIFY section — staging + stub edits)
    - .planning/phases/08-dominance-scoring-strain-role-classification/08-RESEARCH.md (Pitfall 4 snapshot/stub drift; Runtime State Inventory → Build artifacts + Legacy output columns)
  </read_first>
  <action>
    In `modules/local/summarize/main.nf`, add `path(classify_roles)` to the helper `path()` input block (after `path(assembly_support_join)` at L33), mirroring the existing helper staging. At the SUMMARIZE call site in `workflows/hcvtyper.nf` (where `assembly_support_join.R` is passed at L531), add `file("${projectDir}/bin/classify_roles.R")` in the matching argument position.

    Update the module STUB header (Pitfall 4) so the stub's hard-coded `Summary.csv` schema matches the new real output: in the stub `Summary.csv` heredoc header (L72) AND the `summary_mqc.tsv` printf header (L77), REMOVE the trailing `minor_denovo_status` field and APPEND `overall_sample_call` (plus any new wide role columns surfaced in Task 2), and update the single stub data row to add a placeholder value for the new column(s) and drop the removed one so the column count matches the header. Do NOT regenerate the nf-test snapshot in this plan — per MEMORY "Phase 7 SUMMARIZE snapshot regen deferred" and "Host disk near-full", the snapshot regen is folded into the already-pending Phase-7 regen (docker/disk-constrained, invoked via `PATH=~/.nf-test:$PATH nf-test ... --profile docker`); record it in the SUMMARY as a tracked deferred item, not a blocker.
  </action>
  <verify>
    <automated>grep -q 'classify_roles' modules/local/summarize/main.nf && grep -q 'classify_roles' workflows/hcvtyper.nf && grep -q 'overall_sample_call' modules/local/summarize/main.nf && ! grep -q 'minor_denovo_status' modules/local/summarize/main.nf</automated>
  </verify>
  <acceptance_criteria>
    - `modules/local/summarize/main.nf` declares `path(classify_roles)` in the input block
    - `workflows/hcvtyper.nf` passes `file("${projectDir}/bin/classify_roles.R")` at the SUMMARIZE call site
    - The stub `Summary.csv` heredoc header AND the `summary_mqc.tsv` printf header contain `overall_sample_call` and NOT `minor_denovo_status`
    - The stub data row column count matches the updated header (no trailing/short field)
    - SUMMARY records the nf-test snapshot regen as a tracked deferred item folded into the Phase-7 regen (not done here)
  </acceptance_criteria>
  <done>classify_roles.R is staged and passed; the stub schema matches the new Summary.csv (overall_sample_call in, minor_denovo_status out); snapshot regen tracked as deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline-internal CSV/TSV → summarize.R | Upstream candidate/support/depth files; trusted, but may be empty/malformed on no-mapping / skip-assembly runs |
| tracked config params → R positional args | `score_weight_*` / `denovo_*` / `minRead`/`minCov` from version-controlled config; trusted, no external user input |

This is an internal R scoring/classification step over trusted pipeline-internal data with NO new external input surface (no auth, session, network, secret, or external-user-input). Per ASVS L1 with `security_block_on: high`, there are NO high-severity threats. The residual low-severity items are DoS-via-malformed-internal-data and positional-arg config tampering, both covered by existing house patterns.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering (config) | new score_weight_* positional ext.args silently re-mapping existing arg indices | mitigate | Append new args at the END only (after n_candidates) + defensive index-guarded `as.numeric(args[N])` parse with default fallback (Task 1, Pitfall 2) |
| T-08-05 | Denial of Service | malformed/empty upstream candidates/support CSV (no-mapping, skip-assembly) aborting SUMMARIZE | mitigate | Pinned `col_types` on candidate read (CR-01/CR-02 precedent, L519-528) + the classifier's typed zero-row guard (Plan-01 T-08-01) — NA-fill, never stop() |
| T-08-06 | Denial of Service | zero-mean / zero-length coverage candidate producing NaN/Inf cv_evenness | mitigate | Zero-mean / zero-length guard in the cov loop → cv_evenness = 0 (Task 1, Pitfall 3) |
| T-08-SC | Tampering | npm/pip/cargo/conda installs | accept | No package installs this phase — all R libs pre-provisioned in the pinned SUMMARIZE container; no new dependency surface |

No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — Phase 8 installs nothing. `security_block_on: high` satisfied.
</threat_model>

<verification>
- `Rscript -e 'parse("bin/summarize.R")'` succeeds after every task
- `bash bin/tests/run_all.sh` exits 0 (Plan-01 helper tests still green against the integrated helper)
- `grep` confirms: classify_roles staged + passed; overall_sample_call present; minor_denovo_status absent from summarize.R select + module stub; apply_denovo_layer consumption removed
- score_weight_* appended after n_candidates in ext.args (positional integrity)
- nf-test snapshot regen explicitly recorded as deferred (folded into Phase-7 regen) — NOT a Phase-8 gate per CONTEXT scope
</verification>

<success_criteria>
- summarize.R sources + runs classify_roles over the Phase-7-joined candidate frame; each candidate gets role/dominance_score/role_reason; one overall_sample_call per sample
- cv_evenness computed inside the cov loop from cov$X3 (zeros included, zero-mean guarded)
- Legacy apply_denovo_layer / minor_denovo_status / coinfection_flag retired; review_flag rewired onto roles (D-15)
- Backgrounds surfaced in the enriched long *.candidates.csv, never dropped (CLASS-03)
- score_weight_* wired config→ext.args→args without re-mapping n_candidates (Pitfall 2)
- classify_roles.R staged + passed at the SUMMARIZE call site; stub header updated; snapshot regen tracked deferred
- bin/summarize.R parses; R unit suite green
</success_criteria>

<output>
Create `.planning/phases/08-dominance-scoring-strain-role-classification/08-02-SUMMARY.md` when done
</output>
