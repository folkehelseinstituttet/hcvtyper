---
phase: 09-compatibility-filename-migration-regression-suite
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - workflows/hcvtyper.nf
  - modules/local/parsefirstmapping/tests/main.nf.test
  - modules/local/parsefirstmapping/tests/main.nf.test.snap
  - modules/local/blastparse/tests/main.nf.test
  - modules/local/blastparse/tests/main.nf.test.snap
autonomous: true
requirements: [COMPAT-02]
must_haves:
  truths:
    - "The n_candidates > 2 guard is deleted; running with n_candidates=3 no longer errors out"
    - "The workflow fan-out picks each candidate's FASTA by rank from the N-FASTA candidate_fasta emit, not from a fixed major/minor pair"
    - "Single-candidate (monoinfection) samples still survive the fan-out (remainder semantics preserved)"
    - "parsefirstmapping and blastparse nf-tests pass against the renamed cand-slot outputs with regenerated snapshots"
  artifacts:
    - path: "workflows/hcvtyper.nf"
      provides: "N-FASTA rank-indexed fan-out consuming PARSEFIRSTMAPPING.out.candidate_fasta"
      contains: "candidate_fasta"
    - path: "modules/local/parsefirstmapping/tests/main.nf.test.snap"
      provides: "regenerated snapshot with cand-slot filenames"
      contains: "cand"
    - path: "modules/local/blastparse/tests/main.nf.test.snap"
      provides: "regenerated snapshot with cand-slot FASTA filenames"
      contains: "cand"
  key_links:
    - from: "workflows/hcvtyper.nf fan-out"
      to: "PARSEFIRSTMAPPING.out.candidate_fasta"
      via: "join + rank-indexed FASTA lookup"
      pattern: "candidate_fasta"
    - from: "modules/local/parsefirstmapping/tests/main.nf.test"
      to: "renamed cand-slot module outputs"
      via: "nf-test snapshot + assertions"
      pattern: "cand"
---

<objective>
Complete the COMPAT-02 producer migration on the workflow + module-test side (D-01/D-03/D-04). Delete the `n_candidates > 2` guard in `workflows/hcvtyper.nf`, generalize the two-slot FASTA pick (`rank == '1' ? major_fasta : minor_fasta`) to a rank-indexed lookup against the new `PARSEFIRSTMAPPING.out.candidate_fasta` N-FASTA emit, update the parsefirstmapping nf-test assertions that hardcode `.major.fa`/`.minor.fa`, and regenerate both module `.snap` files (parsefirstmapping + blastparse).

Purpose: This is the workflow-routing + test-artifact half of the producer migration. It depends on Plan 01 (the renamed `candidate_fasta` emit and cand-slot FASTAs must exist first). It must preserve the anti-regression invariants (remainder:true, String-typed rank, null-FASTA + confirmation_status guard) or single-candidate samples vanish (RESEARCH Pitfall 3).
Output: N-slot routing wired end-to-end; both module nf-tests green against cand-slot outputs with regenerated snapshots.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-compatibility-filename-migration-regression-suite/09-CONTEXT.md
@.planning/phases/09-compatibility-filename-migration-regression-suite/09-RESEARCH.md
@.planning/phases/09-compatibility-filename-migration-regression-suite/09-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/09-compatibility-filename-migration-regression-suite/09-01-SUMMARY.md
</context>

## Artifacts this phase produces

See 09-01-PLAN.md "Artifacts this phase produces". This plan consumes the `candidate_fasta` emit from Plan 01 and produces:
- Rank-indexed N-FASTA fan-out in `workflows/hcvtyper.nf` (replaces the `rank == '1' ? major_fasta : minor_fasta` two-slot pick).
- Regenerated `.snap` files for parsefirstmapping and blastparse.

<tasks>

<task type="auto">
  <name>Task 1: Delete the n_candidates>2 guard and generalize the N-FASTA fan-out</name>
  <files>workflows/hcvtyper.nf</files>
  <read_first>
    - workflows/hcvtyper.nf guard (L79-87) and fan-out (L395-448)
    - .planning/phases/09-compatibility-filename-migration-regression-suite/09-PATTERNS.md (workflows/hcvtyper.nf section — the 4 patterns that MUST be preserved verbatim)
    - .planning/phases/09-compatibility-filename-migration-regression-suite/09-01-SUMMARY.md (the exact candidate_fasta emit shape Plan 01 produced)
    - modules/local/parsefirstmapping/main.nf (the candidate_fasta emit, to confirm the tuple shape)
  </read_first>
  <action>
    Delete the entire `if (params.n_candidates > 2) { error ... }` guard block (L79-87 including the leading comment) — D-01 lifts it; the error message points at this very phase.
    Generalize the fan-out (L406-444). Replace the two legacy joins (`.join(PARSEFIRSTMAPPING.out.major_mapping, remainder: true)` and `.join(PARSEFIRSTMAPPING.out.minor_mapping, remainder: true)`) with a single `.join(PARSEFIRSTMAPPING.out.candidate_fasta, remainder: true)`. The candidate_fasta emit is `tuple(meta, parsefirstmapping_csv, [list of *_cand*.fa])` (collected per sample). Inside the `flatMap`, after lifting each candidate row into `new_meta`, pick that candidate's FASTA from the collected list by matching the file whose basename contains `_cand${new_meta.candidate_rank}` (e.g. `_cand1.fa` for rank 1). Replace the line `def fasta = (rank == '1') ? major_fasta : minor_fasta` with a rank-indexed lookup over the candidate_fasta path list, e.g. `def fasta = fasta_list?.find { it.toString().contains("_cand${rank}.") }`.
    PRESERVE VERBATIM (anti-regression, Pitfall 3): (1) `remainder: true` on the candidate_fasta join so a single-candidate sample missing higher-rank FASTAs is not dropped; (2) `def rank = new_meta.candidate_rank.toString()` — rank/confirmation_status stay Strings, never `.toInteger()` here; (3) the final `.filter { entry -> entry[0]['confirmation_status'] == 'pass' && entry[1] != null }` null-FASTA + pass guard; (4) the `assert new_meta.id == new_meta.sample` check. Update the explanatory comment block (L395-405, L430-443) to describe the single candidate_fasta emit and the rank-indexed lookup instead of the major/minor shim.
  </action>
  <verify>
    <automated>cd /home/jonbra/hcvtyper && grep -c 'params.n_candidates > 2' workflows/hcvtyper.nf | grep -qx 0 && echo GUARD_GONE; grep -q 'PARSEFIRSTMAPPING.out.candidate_fasta' workflows/hcvtyper.nf && echo USES_NEW_EMIT; grep -c 'major_mapping\|minor_mapping' workflows/hcvtyper.nf | grep -qx 0 && echo NO_LEGACY_EMIT; grep -q "remainder: true" workflows/hcvtyper.nf && grep -q "candidate_rank.toString()" workflows/hcvtyper.nf && echo GUARDS_PRESERVED</automated>
  </verify>
  <acceptance_criteria>
    - No line in workflows/hcvtyper.nf contains `params.n_candidates > 2`
    - The fan-out joins `PARSEFIRSTMAPPING.out.candidate_fasta` and contains no `major_mapping` or `minor_mapping` reference
    - `remainder: true` is present on the candidate_fasta join
    - `new_meta.candidate_rank.toString()` is preserved (rank stays a String in the workflow)
    - The `confirmation_status == 'pass' && entry[1] != null` filter and the `assert new_meta.id == new_meta.sample` line are preserved
    - The FASTA is picked by matching `_cand${rank}` in the path, not by a `rank == '1'` branch
  </acceptance_criteria>
  <done>Guard deleted; fan-out consumes candidate_fasta with a rank-indexed lookup; all four anti-regression invariants preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update parsefirstmapping nf-test assertions to cand-slot</name>
  <files>modules/local/parsefirstmapping/tests/main.nf.test</files>
  <read_first>
    - modules/local/parsefirstmapping/tests/main.nf.test (the snapshot maps reference major_mapping/minor_mapping at L42-46, L79-83, L127-131, L170-174, L228-232; the hardcoded assertions at L119 `2k1b_AB031663_major.fa`, L122/L165 `minor_mapping.size() == 0`)
    - modules/local/parsefirstmapping/main.nf (the new candidate_fasta emit name from Plan 01)
    - .planning/codebase/TESTING.md (nf-test framework, snapshot conventions)
  </read_first>
  <action>
    Update every reference to the removed `major_mapping`/`minor_mapping` emits. In each `snapshot([ ... ]).match()` map (L42-46, L79-83, L127-131, L170-174, L228-232) replace the `major_mapping: process.out.major_mapping` and `minor_mapping: process.out.minor_mapping` entries with a single `candidate_fasta: process.out.candidate_fasta` entry; keep `csv`, `candidates`, `versions` entries.
    Update the hardcoded FASTA assertion at L119 from `process.out.major_mapping.get(0).get(2).toString().contains("2k1b_AB031663_major.fa")` to assert against `process.out.candidate_fasta` and the renamed file `2k1b_AB031663_cand1.fa`.
    Replace the `process.out.minor_mapping.size() == 0` single-candidate assertions (L122, L165) with the equivalent statement on the new emit: a single-candidate sample emits a candidate_fasta tuple whose FASTA list has exactly one `_cand1.fa` entry and no `_cand2.fa`. Assert `process.out.candidate_fasta.get(0).get(2).toString().contains("_cand1.fa")` and `!...contains("_cand2.fa")` (adjust `.get(N)` index to the candidate_fasta tuple arity: meta, parsefirstmapping_csv, fasta_list).
    Do NOT change the idxstats/tsv fixture inputs, the candidates-table assertions (candidate_rank header, row counts), or the GATE-02 cell() assertions on the legacy wide CSV.
  </action>
  <verify>
    <automated>cd /home/jonbra/hcvtyper && grep -c 'major_mapping\|minor_mapping' modules/local/parsefirstmapping/tests/main.nf.test | grep -qx 0 && echo NO_LEGACY_REF; grep -q 'candidate_fasta' modules/local/parsefirstmapping/tests/main.nf.test && echo USES_NEW_EMIT; grep -q '_cand1.fa' modules/local/parsefirstmapping/tests/main.nf.test && echo CAND_ASSERT</automated>
  </verify>
  <acceptance_criteria>
    - No line in modules/local/parsefirstmapping/tests/main.nf.test contains `major_mapping` or `minor_mapping`
    - Each snapshot map references `process.out.candidate_fasta` (single emit) and keeps the `csv`/`candidates`/`versions` entries
    - The hardcoded FASTA assertion targets `2k1b_AB031663_cand1.fa` (not `..._major.fa`)
    - The single-candidate assertion proves exactly one `_cand1.fa` and no `_cand2.fa` on the candidate_fasta tuple
    - The idxstats/tsv fixture inputs, candidate_rank/row-count assertions, and GATE-02 cell() assertions are unchanged
  </acceptance_criteria>
  <done>parsefirstmapping nf-test references the candidate_fasta emit only; the hardcoded `.major.fa`/`.minor.fa` assertions are migrated to `_cand1.fa`/`_cand2.fa`; no legacy emit name remains.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate both module snapshots and diff-guard against unintended churn</name>
  <files>modules/local/parsefirstmapping/tests/main.nf.test.snap, modules/local/blastparse/tests/main.nf.test.snap</files>
  <read_first>
    - modules/local/blastparse/tests/main.nf.test (NOTE: this file has NO `.major.fa`/`.minor.fa` string assertions — the stale slot strings live ONLY in its `.snap`; the `major_fasta`/`minor_fasta` snapshot keys and `toy.major.fa`/`toy.minor.fa` md5 entries are at .snap L26/L34/L80-93. Regenerating the snap after Plan 01's module rename is the only edit blastparse needs — do not hand-edit the .snap.)
    - .planning/phases/09-compatibility-filename-migration-regression-suite/09-RESEARCH.md (Pitfall 4 — `--update-snapshot` is a blanket "accept current output"; the ONLY expected change is `*.major.fa`/`*.minor.fa` → `*.cand1.fa`/`*.cand2.fa`, plus the candidates.csv md5 if its content changed; ANY other md5 churn is a real regression. RESEARCH §"Regenerating module snapshots (D-03)" — use the conda `NEXTFLOW` env, host `nextflow` has a permission error.)
    - .planning/codebase/TESTING.md (nf-test snapshot conventions; never hand-edit md5 values)
  </read_first>
  <action>
    Regenerate BOTH module snapshots against the Plan 01 renamed outputs and the Task 2 assertion changes. Use the conda `NEXTFLOW` env (provides nf-test 0.9.2; the host `nextflow` binary has a permission error per RESEARCH):
    - `conda run -n NEXTFLOW nf-test test modules/local/parsefirstmapping/tests/main.nf.test --profile test,docker --update-snapshot`
    - `conda run -n NEXTFLOW nf-test test modules/local/blastparse/tests/main.nf.test --profile test,docker --update-snapshot`
    BEFORE accepting the regenerated `.snap` files, eyeball the git diff of each (Pitfall 4). The ONLY changes you may accept are the slot-string renames: `*.major.fa`/`*.minor.fa` → `*_cand1.fa`/`*_cand2.fa` (and the `major_fasta`/`minor_fasta` snapshot keys → `candidate_fasta` for parsefirstmapping), plus the `candidates.csv` md5 IF Plan 01 changed its content. If the diff touches any OTHER md5 (stats/depth filenames change shape beyond the slot, an unexpected `candidates.csv` md5 churn, or a versions/png md5 shift), STOP — that is a real regression to investigate, not a snapshot to accept. Do NOT hand-edit md5 values; if a snap is wrong, fix the producing module/test and re-run `--update-snapshot`.
    Re-run both module tests WITHOUT `--update-snapshot` to confirm they pass against the regenerated snapshots.
  </action>
  <verify>
    <automated>cd /home/jonbra/hcvtyper && grep -c 'major\.fa\|minor\.fa\|major_fasta\|minor_fasta' modules/local/parsefirstmapping/tests/main.nf.test.snap modules/local/blastparse/tests/main.nf.test.snap | grep -v ':0$' | grep -q . && { echo LEGACY_SLOT_REMAINS; false; } || echo NO_LEGACY_SLOT; grep -q 'cand1\|cand2\|candidate_fasta' modules/local/blastparse/tests/main.nf.test.snap && echo BLASTPARSE_CAND; conda run -n NEXTFLOW nf-test test modules/local/parsefirstmapping/tests/main.nf.test modules/local/blastparse/tests/main.nf.test --profile test,docker 2>&1 | tee /tmp/snap.log; grep -qi 'PASSED\|SUCCESS' /tmp/snap.log && ! grep -qi 'FAILED' /tmp/snap.log && echo SNAPS_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - Neither `.snap` file contains `major.fa`, `minor.fa`, `major_fasta`, or `minor_fasta` (all slot strings renamed to cand-slot)
    - blastparse `.snap` carries the renamed `*_cand1.fa`/`*_cand2.fa` (or `candidate_fasta`) entries
    - The snap diff is confined to the slot rename (+ candidates.csv md5 only if Plan 01 changed its content) — no other md5 churn was accepted (Pitfall 4)
    - Both module tests PASS without `--update-snapshot` against the regenerated snapshots
  </acceptance_criteria>
  <done>Both module `.snap` files are tool-regenerated with cand-slot filenames, the diff is confined to the expected slot rename (no unexpected md5 churn), and both module nf-tests are green against the new snapshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PARSEFIRSTMAPPING emit → workflow fan-out | The candidate_fasta tuple (meta + per-rank FASTA list) crosses into the Nextflow channel routing; produced upstream in the same pipeline |
| committed `.snap` golden files → CI | Tool-generated md5 fixtures consumed by nf-test in CI; stale slot strings break the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering (silent sample loss) | workflows/hcvtyper.nf fan-out (`remainder: true` + rank String + null-FASTA guard) | mitigate | Preserve all four anti-regression invariants verbatim (Task 1 acceptance_criteria); a dropped `remainder: true` silently deletes monoinfection samples (RESEARCH Pitfall 3) |
| T-09-02-SNAP | Tampering (false-green) | regenerated `.snap` md5 fixtures | mitigate | Task 3 diff-guards `--update-snapshot` output against Pitfall 4 — only slot-rename md5s accepted; any other churn is a flagged regression, never blanket-accepted |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; nf-test runs from the already-pinned `NEXTFLOW` conda env |
</threat_model>

<verification>
- `params.n_candidates > 2` guard is gone from workflows/hcvtyper.nf.
- Fan-out consumes `PARSEFIRSTMAPPING.out.candidate_fasta` with a rank-indexed lookup; no `major_mapping`/`minor_mapping` reference remains.
- All four anti-regression invariants (remainder:true, rank-as-String, null-FASTA + pass filter, id==sample assert) preserved.
- parsefirstmapping nf-test references candidate_fasta only and asserts `_cand1.fa`/`_cand2.fa`.
- Both `.snap` files regenerated with cand-slot filenames; diff confined to the slot rename (Pitfall 4 guard).
- Both module nf-tests pass without `--update-snapshot`.
</verification>

<success_criteria>
The `n_candidates > 2` guard is deleted, the workflow fan-out routes N candidates by rank from the single `candidate_fasta` emit (all four anti-regression invariants intact), the parsefirstmapping nf-test asserts cand-slot filenames against the new emit, and both module `.snap` files are tool-regenerated with the slot rename confirmed to be the ONLY change — both module nf-tests green.
</success_criteria>

<output>
Create `.planning/phases/09-compatibility-filename-migration-regression-suite/09-02-SUMMARY.md` when done
</output>
