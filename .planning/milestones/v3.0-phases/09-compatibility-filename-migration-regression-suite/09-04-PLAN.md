---
phase: 09-compatibility-filename-migration-regression-suite
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - bin/tests/test_compat.R
  - bin/tests/fixtures/compat_golden.csv
autonomous: false
requirements: [COMPAT-01, COMPAT-03, COMPAT-04, TEST-01]
must_haves:
  truths:
    - "Running `bash bin/tests/run_all.sh` in the pinned R container discovers and runs test_compat.R (glob auto-discovery, no CI YAML edit)"
    - "test_compat.R asserts the core strain-call columns (Major_reference, Minor_reference, Major_genotype_mapping, Minor_genotype_mapping, overall_sample_call) reproduce the v1.0/v2.0 golden values for a monoinfection and a co-infection case (COMPAT-01)"
    - "test_compat.R drives summarize.R with synthetic *.cand1.*/*.cand2.* stats files and asserts Major_*/Minor_* rows are populated (proves the candidate_rank lockstep, COMPAT-02 smoke)"
    - "test_compat.R asserts Summary.csv carries the legacy Major_*/Minor_* columns alongside the new Major_role_*/Minor_role_* columns (COMPAT-03, validate-only)"
    - "test_compat.R asserts via the full summarize.R/classify_roles path that a 1a/1b co-infection is allowed and a 2k/1b recombinant is suppressed (COMPAT-04 end-to-end)"
  artifacts:
    - path: "bin/tests/test_compat.R"
      provides: "regression suite covering COMPAT-01/02/03/04, auto-discovered by run_all.sh"
      contains: "ALL PASS"
    - path: "bin/tests/fixtures/compat_golden.csv"
      provides: "human-verified v1.0/v2.0 golden strain-call anchor for the COMPAT-01 cases (D-07)"
      contains: "overall_sample_call"
  key_links:
    - from: "bin/tests/run_all.sh"
      to: "bin/tests/test_compat.R"
      via: "test_*.R glob auto-discovery"
      pattern: "test_compat"
    - from: "bin/tests/test_compat.R"
      to: "bin/summarize.R"
      via: "system2('Rscript', ...) subprocess invocation on staged path_N subdirs"
      pattern: "summarize.R"
    - from: "bin/tests/test_compat.R"
      to: "bin/classify_roles.R"
      via: "source() + classify_roles(score_candidates(...)) for COMPAT-04"
      pattern: "classify_roles"
---

<objective>
Add the Phase 9 regression suite: one new `bin/tests/test_compat.R` (D-08) covering all four COMPAT requirements that have no dedicated test yet (COMPAT-01 golden reproduction, COMPAT-03 legacy-column validation, COMPAT-04 1a/1b-allowed / 2k/1b-suppressed end-to-end). The file is auto-discovered by the `run_all.sh` glob and runs in the existing `r-regression` CI job in the pinned Seqera container — no CI YAML edit (TEST-01). It also includes the COMPAT-02 lockstep smoke-test (synthetic `.cand1.`/`.cand2.` stats files → populated Major_*/Minor_*), which only passes once the Plan 01/03 producer+consumer rename and the Plan 02 workflow fan-out are in place — hence Wave 3, after both.

Purpose: This is the validation half of Phase 9. The producer/consumer rename (Plans 01-03) is silent-failure-prone (RESEARCH Pitfall 1 — a config/summarize.R slot mismatch empties Major_*/Minor_* with no crash); this test is the executable proof that nothing regressed. COMPAT-04 helper-level coverage already exists in `test_classify_roles.R`; this plan adds the full-path integration case.
Output: `bin/tests/test_compat.R` (green under `run_all.sh` in the R container) + a human-verified `compat_golden.csv` anchor for the two COMPAT-01 cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-compatibility-filename-migration-regression-suite/09-CONTEXT.md
@.planning/phases/09-compatibility-filename-migration-regression-suite/09-RESEARCH.md
@.planning/phases/09-compatibility-filename-migration-regression-suite/09-PATTERNS.md
@.planning/phases/09-compatibility-filename-migration-regression-suite/09-01-SUMMARY.md
@.planning/phases/09-compatibility-filename-migration-regression-suite/09-03-SUMMARY.md
</context>

## Artifacts this phase produces

See 09-01-PLAN.md "Artifacts this phase produces". This plan adds the final symbol from that list:
- `bin/tests/test_compat.R` — new regression test file (D-08) covering COMPAT-01/02/03/04, auto-discovered by `run_all.sh`.
- `bin/tests/fixtures/compat_golden.csv` — new human-verified golden anchor (D-06/D-07) holding the expected core strain-call columns for the two COMPAT-01 cases.

This plan CONSUMES (does not modify): the Plan 01 `.cand{rank}.` slot + per-rank FASTA naming, the Plan 03 `candidate_rank`-join parse in `bin/summarize.R`, the Plan 02 N-FASTA workflow fan-out, and the already-shipped Phase 8 `bin/classify_roles.R` (`score_candidates`, `classify_roles`, `is_valid_minor`).

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm + lock the COMPAT-01 golden strain-call values (D-06/D-07)</name>
  <files>bin/tests/fixtures/compat_golden.csv</files>
  <action>
    Draft `bin/tests/fixtures/compat_golden.csv` (two rows: a monoinfection case and a co-infection case) holding the proposed expected values for the five core strain-call columns (`Major_reference`, `Minor_reference`, `Major_genotype_mapping`, `Minor_genotype_mapping`, `overall_sample_call`), then PAUSE for human confirmation before Task 2 locks any assertion against it. Do NOT invent the baseline — RESEARCH A2/Open-Q1 flags that `flagoff_golden.csv` may not equal v1.0/v2.0 output for these exact cases. Present the drafted values per the <what-built> block (the exact reference accession strings + the five-column row contents) and wait for the <resume-signal>. If the human supplies corrected values or reference accessions, write those into compat_golden.csv before continuing.
  </action>
  <what-built>
    Before any assertion is written, the two COMPAT-01 golden cases (D-07) must be anchored to real v1.0/v2.0 expected output, not invented. RESEARCH Assumption A2 / Open-Q1 flags that reusing `bin/tests/fixtures/flagoff_golden.csv` may NOT equal what v1.0/v2.0 emits for these exact cases — especially `Major_genotype_mapping` and `overall_sample_call`, which `flagoff_golden.csv` does not even contain (it only has Major_reference/Minor_reference/major_typable/minor_typable).
    The executor will draft `bin/tests/fixtures/compat_golden.csv` with two rows holding the proposed expected values for the five core strain-call columns:
    - Row (a) monoinfection: a 1a-dominant sample, no minor — `overall_sample_call = monoinfection`, `Major_reference` = the 1a ref, `Major_genotype_mapping = 1`, `Minor_reference = NA`, `Minor_genotype_mapping = NA`.
    - Row (b) co-infection: a 1a-dominant + 1b-corroborated sample — `overall_sample_call = co-infection`, both `Major_reference`/`Minor_reference` populated, `Major_genotype_mapping = 1`, `Minor_genotype_mapping = 1`.
    The executor presents the drafted column values (the exact reference accession strings + the five-column row contents) for confirmation.
  </what-built>
  <how-to-verify>
    1. Open the drafted `bin/tests/fixtures/compat_golden.csv` (the executor will print it).
    2. Compare each of the five core strain-call columns against a known v1.0/v2.0 reference: the handoff evidence table (`.planning/` handoff materials referenced in 09-RESEARCH §"State of the Art"), `bin/tests/fixtures/flagoff_golden.csv`, or a v1.0/v2.0 reference run if available.
    3. Confirm the reference accession strings used for the 1a major and 1b minor are real entries from the pipeline's references DB (so the synthetic candidates CSV the test feeds summarize.R uses genotype-correct refs — `genotype_from_subtype()` must derive `1` for both).
    4. Confirm `overall_sample_call` is exactly `monoinfection` for (a) and `co-infection` for (b) (these are the literal values summarize.R writes — see bin/summarize.R L1279).
  </how-to-verify>
  <verify>
    <human-check>Human confirms the five core strain-call column values in compat_golden.csv match a v1.0/v2.0 reference (handoff evidence table / flagoff_golden.csv / reference run) and the reference accessions are real genotype-1 entries.</human-check>
  </verify>
  <resume-signal>Type "approved" to lock compat_golden.csv as the COMPAT-01 baseline, or provide the corrected column values / reference accessions to use.</resume-signal>
  <done>compat_golden.csv exists with two human-confirmed rows (monoinfection + co-infection) whose five core strain-call columns are the locked COMPAT-01 baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Write the COMPAT-01/02/03 subprocess cases (drive summarize.R end-to-end)</name>
  <files>bin/tests/test_compat.R, bin/tests/fixtures/compat_golden.csv</files>
  <read_first>
    - bin/tests/fixtures/compat_golden.csv — MUST have human-approved values from the Task 1 checkpoint before this task begins; this task locks assertions against those values and must not run until the checkpoint is "approved".
    - bin/tests/test_candidate_selection.R (L46-51 entrypoint boilerplate; L64-109 the tempdir + system2 subprocess harness to model — note it copies genotype_utils.R into the cwd and passes absolute fixture paths)
    - bin/summarize.R L27-86 (the FULL positional arg contract: args[1]=samplesheet, [2]=version, [3]=name, [4..8] denovo params, [9]=minRead, [10]=minCov, [11]=n_candidates, [12-15] score weights) AND the hardcoded input subdirs path_1..path_11/path_denovo (L76-86: trimmed/, kraken_classified/, parsefirst_mapping/, stats_withdup/, stats_markdup/, depth/, denovo/, glue/, id/, variation/, consensus_distance/)
    - bin/summarize.R L188-209 (parsefirstmapping.csv read), L246-306 + L310-363 + L377-492 (the three stats/cov loops the test feeds .cand{rank}. files into), L562-594 (candidates_long load — the candidate_rank join source), L678 + L687-738 (candidates.csv + Major_role_*/Minor_role_* wide block), L1279-1359 (final select + write_csv Summary.csv)
    - bin/tests/test_candidate_selection.R L114-116 (the script's own .candidates.csv / .parsefirstmapping.csv output schema — the test must produce candidates.csv rows with these exact columns: sample, candidate_rank, candidate_ref, candidate_subtype, candidate_genotype, candidate_reads, candidate_cov, confirmation_status)
    - .planning/phases/09-compatibility-filename-migration-regression-suite/09-03-SUMMARY.md (the exact candidate_rank-join behavior Plan 03 shipped — confirms how summarize.R now recovers rank from candidates_long via candidate_ref)
    - .planning/phases/09-compatibility-filename-migration-regression-suite/09-PATTERNS.md (Pattern 3 glob-discovered test; the byte-identical Major_reference/Minor_reference join-key constraint)
  </read_first>
  <action>
    Create `bin/tests/test_compat.R` with the standard entrypoint boilerplate from test_candidate_selection.R L46-51 (`args_all`/`file_arg`/`this_dir`/`bin_dir`), the `fail()`/`ok()` helpers, and `suppressPackageStartupMessages(library(tidyverse))`. Resolve `script <- file.path(bin_dir, "summarize.R")`.
    Build a subprocess harness `run_summarize(case, ...)` modeled on test_candidate_selection.R `run_select`: create an isolated tempdir, `setwd` into it (with `on.exit(setwd(old))`), and create the input subdirs summarize.R reads from (`parsefirst_mapping/`, `stats_withdup/`, `stats_markdup/`, `depth/` — these four are sufficient for the core strain-call columns; leave the others absent, the script guards every `list.files` with `if (length(...) > 0)`). Copy `genotype_utils.R`, `denovo_confirm.R`, `denovo_layer.R`, `assembly_support_join.R`, and `classify_roles.R` from `bin_dir` into the tempdir cwd, because summarize.R `source()`s all five relatively (bin/summarize.R L10-25). Write a one-row samplesheet CSV (the script reads it at L1028 — read bin/summarize.R L1028-1050 to match the required samplesheet columns; pass its absolute path as args[1]).
    Stage the per-candidate fixtures using the NEW slot naming so this doubles as the COMPAT-02 lockstep smoke-test: write `parsefirst_mapping/<sample>.candidates.csv` (one row per candidate with the 8-column long schema; candidate_rank 1 = the 1a major, rank 2 = the 1b minor) and `parsefirst_mapping/<sample>.parsefirstmapping.csv` (the legacy 10-col shim). Write `stats_withdup/<sample>.<ref>.cand1.withdup.stats` and `...cand2.withdup.stats`, the matching `stats_markdup/...cand{rank}.nodup.stats`, and `depth/...cand{rank}.nodup.tsv` files. The stats files are samtools-stats text the loop parses with `read_tsv(comment="#")` — read bin/summarize.R L264-306 to see exactly which `SN`/metric lines the loop pulls, and emit only those lines so the parse populates reads/cov. The depth tsv is `<ref> <pos> <cov>` (3-col, no header) per bin/summarize.R L396. Use `cand1`/`cand2` in BOTH the filename slot AND the `candidate_ref`-vs-filename relationship so the Plan 03 `_cand[0-9]+$` strip + candidate_rank join resolves: the filename `<ref>.cand{rank}` must reduce to the `candidate_ref` token present in candidates.csv.
    Invoke via `system2("Rscript", c(shQuote(script), shQuote(samplesheet), "TestPipe", "hcvtyper", "", "", "", "", "", minRead, minCov, n_candidates), stdout=FALSE, stderr=FALSE)` (positions [4-8] empty-string to take defaults; minRead/minCov/n_candidates at [9]/[10]/[11]). Read back `Summary.csv` from the cwd.
    Write three assertion blocks:
    - COMPAT-01 (D-06/D-07): load `bin/tests/fixtures/compat_golden.csv` (absolute path via `file.path(this_dir, "fixtures", "compat_golden.csv")`). Run the monoinfection case and the co-infection case; for each, assert the five core columns (`Major_reference`, `Minor_reference`, `Major_genotype_mapping`, `Minor_genotype_mapping`, `overall_sample_call`) in the produced Summary.csv equal the golden row. Do NOT assert on role/dominance_score/score columns (D-06 — additive, not part of the comparison). Treat NA-vs-NA as equal for the monoinfection Minor_* cells.
    - COMPAT-02 smoke: assert the co-infection case's Summary.csv has a NON-NA `Major_reference` AND `Minor_reference` (proves the `.cand1.`/`.cand2.` filenames parsed via the candidate_rank join and did not silently empty — RESEARCH Pitfall 1). Assert `Major_reference` equals the cleaned ref token with NO `_cand1`/`.cand1` suffix (byte-identical join-key constraint, 09-PATTERNS).
    - COMPAT-03 (D-05): assert the Summary.csv column set CONTAINS the legacy columns `Major_reference`, `Minor_reference`, `Major_genotype_mapping`, `Minor_genotype_mapping` AND the new role columns `Major_role_reference`, `Minor_role_reference`, `overall_sample_call` simultaneously (validate-only — do not add or rename any column). Use `all(c(...) %in% colnames(summary))`.
    End the file with `cat("\nALL PASS\n")`. Every failed assertion calls `fail()` which `quit(status=1)` so `run_all.sh` flips its accumulator.
  </action>
  <verify>
    <automated>cd /home/jonbra/hcvtyper && docker run --rm -v "$PWD":/work -w /work community.wave.seqera.io/library/r-seqinr_r-tidyverse:5358395134867368 Rscript -e 'parse("/work/bin/tests/test_compat.R")' && docker run --rm -v "$PWD":/work -w /work community.wave.seqera.io/library/r-seqinr_r-tidyverse:5358395134867368 Rscript bin/tests/test_compat.R 2>&1 | tee /tmp/compat.log; grep -q 'ALL PASS' /tmp/compat.log && echo COMPAT_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `bin/tests/test_compat.R` exists and uses the test_candidate_selection.R entrypoint boilerplate (args_all/file_arg/this_dir/bin_dir)
    - It invokes the REAL `bin/summarize.R` via `system2("Rscript", ...)` against staged `parsefirst_mapping/`, `stats_withdup/`, `stats_markdup/`, `depth/` subdirs in a tempdir
    - Synthetic stats/depth fixtures use the NEW `.cand1.`/`.cand2.` slot (not `.major.`/`.minor.`)
    - COMPAT-01 block compares the five core strain-call columns against `bin/tests/fixtures/compat_golden.csv` for both the monoinfection and co-infection cases
    - COMPAT-02 smoke block asserts the co-infection Major_reference/Minor_reference are non-NA and carry NO slot suffix
    - COMPAT-03 block asserts both legacy `Major_*`/`Minor_*` and new `Major_role_*`/`Minor_role_*`/`overall_sample_call` columns are present in Summary.csv (no column added/renamed)
    - Running the file in the pinned R container prints `ALL PASS` and exits 0
  </acceptance_criteria>
  <done>test_compat.R drives summarize.R end-to-end on cand-slot fixtures and the COMPAT-01/02/03 assertions pass green in the pinned container.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the COMPAT-04 end-to-end integration case (1a/1b allowed, 2k/1b suppressed)</name>
  <files>bin/tests/test_compat.R</files>
  <behavior>
    - A 1a-dominant + 1b-corroborated sample run through the full score_candidates → classify_roles path yields the 1b candidate role == "co-infection" (the D-12 1a/1b cross-subtype-within-gt1 allowance, exercised end-to-end not just at the helper level).
    - A genotype-2-dominant + 2k1b-second sample run through the same path yields the 2k1b candidate role == "background" with role_reason == "recombinant_2k1b" (the recombinant suppression exception preserved).
    - overall_sample_call is "co-infection" for the 1a/1b sample and not "co-infection" for the 2k1b-suppressed sample.
  </behavior>
  <read_first>
    - bin/tests/test_classify_roles.R (L45-67 the mk_cand builder + the `classify()` wrapper sourcing the REAL helpers; L122-132 the sim1 1a:1b allowance case; L173-184 the 2k1b suppression case — model these two exactly)
    - bin/classify_roles.R (the `is_valid_minor()` 1a/1b allowance + 2k1b recombinant exception this case must exercise; confirm score_candidates/classify_roles signatures)
    - bin/summarize.R L637-738 (how summarize.R itself calls score_candidates+classify_roles and rolls role/overall_sample_call into the wide block — so the integration case mirrors the production call site, not a divergent re-implementation)
  </read_first>
  <action>
    Append a COMPAT-04 block to `bin/tests/test_compat.R`. Per CONTEXT D-08, this exercises the FULL summarize.R path rather than only the helper, complementing the helper-level coverage already in test_classify_roles.R. Prefer the lower-friction approach (RESEARCH Open-Q / discretion A4): reuse the Task-2 `run_summarize()` subprocess harness to feed a 1a/1b co-infection candidates fixture and a 2k1b-second candidates fixture through summarize.R, then read back Summary.csv and assert role outcomes. If the subprocess harness cannot surface per-candidate `role`/`role_reason` (because the wide Summary.csv only carries the dominant + corroborated-minor role slots — bin/summarize.R L687-738), assert on the OBSERVABLE Summary.csv signal instead: for the 1a/1b case assert `overall_sample_call == "co-infection"` AND `Minor_role_reference` is populated with the 1b ref; for the 2k1b case assert `overall_sample_call != "co-infection"` AND the 2k1b ref does NOT appear in `Minor_role_reference` (it was suppressed to background).
    To also lock the exact role/role_reason values (which the wide CSV may not expose), additionally source the helpers directly — `source(file.path(bin_dir, "genotype_utils.R"))`, `source(file.path(bin_dir, "classify_roles.R"))` — and add a function-level mirror of test_classify_roles.R's sim1 (1a/1b → co-infection) and 2k1b (→ background/recombinant_2k1b) cases using a local `mk_cand` builder and a `classify()` wrapper (minRead=500, minCov=30, denovo floors 1000/2.0/90, match_level="genotype"). Assert `role == "co-infection"` for the 1b candidate and `role == "background"` && `role_reason == "recombinant_2k1b"` for the 2k1b candidate. This gives both the end-to-end Summary.csv signal AND the precise role-vocabulary assertion in one COMPAT-04 block.
    Do NOT duplicate the broader test_classify_roles.R suite (false-4g, IVT, etc.) — COMPAT-04 is scoped to exactly the 1a/1b-allowed and 2k/1b-suppressed exceptions.
  </action>
  <verify>
    <automated>cd /home/jonbra/hcvtyper && docker run --rm -v "$PWD":/work -w /work community.wave.seqera.io/library/r-seqinr_r-tidyverse:5358395134867368 Rscript -e 'parse("/work/bin/tests/test_compat.R")' && docker run --rm -v "$PWD":/work -w /work community.wave.seqera.io/library/r-seqinr_r-tidyverse:5358395134867368 Rscript bin/tests/test_compat.R 2>&1 | tee /tmp/compat4.log; grep -q 'ALL PASS' /tmp/compat4.log && grep -qi 'COMPAT-04\|2k1b\|1a/1b\|co-infection' /tmp/compat4.log && echo COMPAT4_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - bin/tests/test_compat.R contains a COMPAT-04 block exercising both the 1a/1b-allowed and 2k/1b-suppressed exceptions
    - The 1a/1b case asserts the 1b candidate is co-infection (role == "co-infection") and the sample overall_sample_call == "co-infection"
    - The 2k1b case asserts the 2k1b candidate is background with role_reason == "recombinant_2k1b" and is NOT reported as a co-infection minor
    - The block uses the real bin/classify_roles.R helpers (score_candidates + classify_roles), not an inline re-implementation
    - The whole file still prints `ALL PASS` and exits 0 in the pinned container
  </acceptance_criteria>
  <done>COMPAT-04 is asserted end-to-end: 1a/1b preserved as co-infection, 2k/1b suppressed to background/recombinant_2k1b; test_compat.R is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidates CSV / stats files → summarize.R | The test stages synthetic candidate_rank/candidate_ref + cand-slot stats files that summarize.R consumes; same boundary the production pipeline crosses, exercised here with controlled fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering (silent data loss) | summarize.R candidate_rank join (Plan 03) under the renamed cand-slot | mitigate | The COMPAT-02 smoke assertion in Task 2 fails loudly if `.cand1.`/`.cand2.` filenames silently empty Major_*/Minor_* (RESEARCH Pitfall 1) — this test IS the executable guard for the highest-risk integration site |
| T-09-04-FP | Repudiation (false-green baseline) | COMPAT-01 golden values | mitigate | Task 1 blocking human-verify checkpoint confirms golden values against a v1.0/v2.0 reference before assertions lock (RESEARCH A2 / Open-Q1) — never auto-approvable |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A); test runs in the already-pinned Seqera container |
</threat_model>

<verification>
- `docker run --rm -v "$PWD":/work -w /work community.wave.seqera.io/library/r-seqinr_r-tidyverse:5358395134867368 bash bin/tests/run_all.sh` runs test_compat.R (auto-discovered) and the full suite stays green.
- test_compat.R prints `ALL PASS` standalone in the pinned container.
- COMPAT-01: five core strain-call columns match compat_golden.csv for monoinfection + co-infection cases.
- COMPAT-02 smoke: cand-slot filenames produce populated, suffix-free Major_reference/Minor_reference.
- COMPAT-03: Summary.csv carries legacy Major_*/Minor_* alongside Major_role_*/Minor_role_*/overall_sample_call.
- COMPAT-04: 1a/1b co-infection preserved, 2k/1b suppressed to background/recombinant_2k1b.
</verification>

<success_criteria>
A single `bin/tests/test_compat.R` covers COMPAT-01 (golden core strain-call reproduction, human-verified anchor), COMPAT-02 (cand-slot lockstep smoke), COMPAT-03 (legacy + role column co-presence, validate-only), and COMPAT-04 (1a/1b allowed, 2k/1b suppressed end-to-end), is auto-discovered by `run_all.sh`, runs green in the existing `r-regression` CI container with no YAML change (TEST-01), and the golden baseline values were confirmed against a v1.0/v2.0 reference before locking.
</success_criteria>

<output>
Create `.planning/phases/09-compatibility-filename-migration-regression-suite/09-04-SUMMARY.md` when done
</output>
