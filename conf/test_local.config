// Local test override — cap high-CPU processes to fit within 8 CPU / 60 GB
process {
    withLabel: 'process_high' {
        cpus   = 8
        memory = { 60.GB * task.attempt }
    }
    withLabel: 'process_medium' {
        cpus   = 6
        memory = { 36.GB * task.attempt }
    }
}
