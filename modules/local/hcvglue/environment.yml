name: hcvglue
# Container-only module: the hcvglue-allinone image bundles MySQL 5.7 + the GLUE
# engine. No conda packages are required; the legacy docker-in-docker conda
# dependencies were removed in the Phase 05 refactor (D-13).
channels:
  - conda-forge
dependencies: []
