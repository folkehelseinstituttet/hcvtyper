// Per-module nf-test config for RESCUE_EVALUATION.
// Structural clone of modules/local/blastparse/tests/nextflow.config.
profiles {
  docker {
    docker.enabled = true
  }
  conda {
    conda.enabled = true
  }
}
process {
  executor = 'local'
  cache = false
}
